@model A2_SinhToDau_QuanLyBenhVien.Models.Specialist
@{
    ViewData["Title"] = "EditSpecialist";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="container px-4">
    <h2 class="mt-3 mb-3 ml-3">Sửa Thông Tin Chuyên Khoa</h2>
    <hr />
    <div class="row mb-4">
        <div class="col-md-6 offset-md-3">
            <form asp-action="EditSpecialist" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />
                <div class="form-group mb-2">
                    <label asp-for="SpecialistName" class="control-label">Tên Chuyên Ngành</label>
                    <input asp-for="SpecialistName" class="form-control" />
                    <span asp-validation-for="SpecialistName" class="text-danger"></span>
                </div>

                <div class="form-group mb-2">
                    <label asp-for="SpecialistDescription" class="control-label">Mô tả</label>
                    <input asp-for="SpecialistDescription" class="form-control" />
                    <span asp-validation-for="SpecialistDescription" class="text-danger"></span>
                </div>

                <div class="form-group mb-2">
                    <label asp-for="SpecialistImg" class="control-label">Ảnh</label>
                    <input type="file" asp-for="SpecialistImg" class="form-control" id="imageInput" />
                    <span asp-validation-for="SpecialistImg" class="text-danger"></span>
                    <div class="form-group mb-1 mt-1">
                        <img id="previewImage" style="max-width: 200px; display: none;" alt="Image preview" />
                    </div>
                </div>
                <br />
                <button type="submit" class="btn btn-primary">Cập Nhật</button>
            </form>
            <script>
                document.getElementById('imageInput').addEventListener('change', function (event) {

                    var output = document.getElementById('previewImage');

                    if (this.files && this.files[0]) {

                        var reader = new FileReader();

                        reader.onload = function (e) {

                            output.src = e.target.result;
                            output.style.display = 'block';
                        };

                        reader.readAsDataURL(this.files[0]);
                    }

                });

            </script>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

